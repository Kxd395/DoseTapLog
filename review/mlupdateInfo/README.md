# mlupdateInfo Folder

Drop **all verification evidence** requested in `REQUEST_PROMPT.md` into this directory.
Use `evidence_manifest.json` (see schema) as the final checklist and index of artifacts.
