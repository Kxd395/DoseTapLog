# Remediation PR Plan

| PR | Scope & Goal | Key Changes | Validation |
|----|--------------|-------------|------------|
| 1 | **Adopt new SwiftUI module as the shipping target** | Update `DoseTrackIOS.xcodeproj` (or create an SPM package) so the app builds from `ios/*.swift`, remove duplicates under `DoseTrackIOS/`, reconfigure build phases/resources, and ensure assets & entitlements carry over. | `xcodebuild -scheme DoseTrackIOS -destination 'platform=iOS Simulator,name=iPhone 17' build`; run UI smoke on simulator. |
| 2 | **Fix TodayLogView compile errors and late-dose flow** | Patch `ios/TodayLogView.swift` to call `vm.logDose1Now(grams:)`, consolidate `TodayViewModel`/`TodayViewModel+LateDose`, extend `DoseLogControllering` to accept override metadata, and persist overrides into `DoseLog`. | Re-run iOS build; add unit tests covering early/late overrides and CSV output. |
| 3 | **Wire Health/WHOOP status and notification toggles** | Implement real permission checks in `StatusChips`, hook `HealthKitManager` and WHOOP proxy client, honor notification toggles (quiet hours, haptics), and surface wake-source state. | Add integration tests/mocks for HealthKit and proxy client; verify UI updates with simulated states. |
| 4 | **Align persistence layer with SQL schema & events** | Replace the simplified `DoseLog` model with entities that match `sleep_sessions`, `medication_events`, `event_log`, etc.; add migrations and ensure controller writes through to the normalized schema. | Run `sqlite3` migrations + `SCHEMA_AUDIT.sql`; execute regression tests for logging, guardrails, and exports. |
| 5 | **Complete settings-driven behaviours** | Respect export settings in `CSVExporter`, implement snooze, health autofill, WHOOP test buttons, and ensure all app preferences in `AppPreferencesEnhanced` drive behaviour. | Add feature tests (unit/UI) for export permutations, snooze actions, and WHOOP diagnostics. |
| 6 | **Stabilize WHOOP proxy automation** | Update `test-server.js` to skip/mocks when `WHOOP_TOKEN` missing, document required env vars, and ensure auth-negative cases assert for `401` without failing. | `npm test` (with and without token); manual `curl` verification. |
