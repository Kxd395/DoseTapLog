# WHOOP proxy configuration (copy to .env and fill with secure values)
API_KEY=changeme-local-dev-key
WHOOP_TOKEN=replace-with-whoop-developer-token
WHOOP_BASE=https://api.prod.whoop.com
PORT=3000
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60
